# Copy this file to .env and fill in your values
# cp .env.example .env

# VNC password for the desktop session
VNC_PASSWORD=change-me

# IP to bind VNC port on (defaults to 127.0.0.1 if not set)
# Set to your LAN IP to allow local network access, e.g. 192.168.1.x
BIND_IP=127.0.0.1
